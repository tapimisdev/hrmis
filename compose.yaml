services:
  server:
    build:
      context: .
    container_name: orbit
    ports:
      - "8080:80"
    # depends_on:
    #   - db
    environment:
      # APP_ENV: local
      # APP_DEBUG: "true"
      # DB_CONNECTION: mysql
      # DB_HOST: db
      # DB_PORT: 3306
      # DB_DATABASE: orbit
      # DB_USERNAME: dost
      # DB_PASSWORD: dost@2025

      REVERB_APP_ID: 403007
      REVERB_APP_KEY: qcwktiocxzrqgecorxqx
      REVERB_APP_SECRET: hcarbiqbpq3op6yiog0f
      REVERB_HOST: server
      REVERB_PORT: 8080
      REVERB_SCHEME: http

    volumes:
      - .:/var/www/html:cached
      - laravel_storage:/var/www/html/storage
      - laravel_storage:/var/www/storage
      - laravel_cache:/var/www/html/bootstrap/cache

  # db:
  #   image: mysql:8.1
  #   container_name: orbit_db
  #   restart: unless-stopped
  #   environment:
  #     MYSQL_ROOT_PASSWORD: dost2025
  #     MYSQL_DATABASE: orbit
  #     MYSQL_USER: dost
  #     MYSQL_PASSWORD: dost@2025
  #   ports:
  #     - "3307:3306"
  #   volumes:
  #     - dbdata:/var/lib/mysql

volumes:
  dbdata:
  laravel_storage:
  laravel_cache:
